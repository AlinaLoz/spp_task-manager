//import {ACTIONS} from "../constans";

const initState = {
};

export default function teams(state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}